# ============================================================
# Hanggent Community Edition — Environment Variables
# ============================================================
# Copy this file to .env and fill in the values.
# Only VITE_EDITION is required; the rest are optional.
# ============================================================

# Edition: "community" (default) or "cloud"
VITE_EDITION=community

# Backend API URL (default: http://localhost:5001)
# VITE_BACKEND_URL=http://localhost:5001

# Server API URL — only needed if running the server layer
# VITE_PROXY_URL=http://localhost:3001

# ── Optional: Clerk Auth (cloud edition only) ──────────────
# VITE_CLERK_PUBLISHABLE_KEY=
# VITE_CLERK_PROXY_URL=

# ── Optional: Analytics ────────────────────────────────────
# VITE_AMPLITUDE_KEY=
